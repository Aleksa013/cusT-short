# cusT-short (Angular + Laravel + Docker)

### 🚀 Как запустить быстро
1. Клонируй репозиторий.
2. Создай `.env` в папке `/backend` (скопируй из `.env.example`).
3. Выполни команду в корне:
   ```bash docker-compose up -d```

### 🌍 Доступ к приложению
*   **Frontend:** [http://localhost:4200](http://localhost:4200)
*   **Backend (API):** [http://localhost:8001](http://localhost:8000)
*   **Database:** `localhost:3307` (User: `dev`, Pass: `pas`)

### 🛠 Стек технологий
*   **Frontend:** Angular 19, Angular Material/Tailwind
*   **Backend:** Laravel 12,PHP 8.2, MySQL 8
*   **Infrastructure:** Docker Compose